# Архитектура проекта

## 🏗️ Общая структура

```
┌─────────────────────────────────────────────────────────┐
│                    matusevich.dev                        │
│              Next.js 15 + TypeScript + Tailwind          │
└─────────────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
    ┌───▼───┐         ┌────▼────┐       ┌────▼─────┐
    │   /   │         │ /cases  │       │ /speaker │
    │ Home  │         │ Кейсы   │       │ Спикер   │
    └───────┘         └─────────┘       └──────────┘
```

## 📐 Главная страница (/)

```
┌────────────────────────────────────────┐
│  Header (sticky)                       │ ← Навигация + CTA
├────────────────────────────────────────┤
│  Hero                                  │ ← Позиционирование + CTA
├────────────────────────────────────────┤
│  Results (8 карточек)                  │ ← Big numbers
├────────────────────────────────────────┤
│  Cases (3 краткие)                     │ ← Problem/Action/Impact
├────────────────────────────────────────┤
│  AI Enablement (3 колонки)             │ ← Support/Eng/Legal
├────────────────────────────────────────┤
│  Approach (6 шагов)                    │ ← Методология
├────────────────────────────────────────┤
│  Formats (4 формата)                   │ ← Способы работы
├────────────────────────────────────────┤
│  Credibility                           │ ← Опыт + сертификаты
├────────────────────────────────────────┤
│  Final CTA                             │ ← Telegram + Email
└────────────────────────────────────────┘
```

## 🧩 Компонентная структура

```
App (page.tsx)
│
├── Header
│   ├── Desktop Navigation (md+)
│   ├── Mobile Menu (< md)
│   └── CTA Button
│
├── Hero
│   ├── H1 Title
│   ├── Description
│   ├── Badges (credentials)
│   └── 2 CTA Buttons
│
├── Results
│   └── 8x Metric Cards
│       ├── Big Number
│       └── Description
│
├── Cases
│   ├── 3x Case Cards
│   │   ├── Problem
│   │   ├── Solution
│   │   ├── Effect
│   │   └── Tools
│   └── "All Cases" CTA
│
├── AIEnablement
│   └── 3x Area Cards
│       ├── Title
│       ├── Description
│       └── Results
│
├── Approach
│   └── 6x Step Cards
│       ├── Number
│       ├── Title
│       └── Description
│
├── Formats
│   ├── 4x Format Cards
│   │   ├── Title
│   │   ├── Description
│   │   └── Tags
│   └── "Speaker Kit" CTA
│
├── Credibility
│   ├── Companies List
│   └── Certifications
│
└── FinalCTA
    ├── Telegram CTA
    ├── Copy-to-clipboard
    └── Email (hidden)
```

## 🗂️ Файловая структура

```
src/app/
│
├── layout.tsx              # Root layout + SEO metadata
├── page.tsx                # Home page (композиция компонентов)
├── globals.css             # Глобальные стили
├── sitemap.ts              # XML sitemap generator
├── robots.ts               # robots.txt generator
│
├── components/             # Все компоненты
│   ├── Header.tsx          # 189 строк
│   ├── Hero.tsx            # 69 строк
│   ├── Results.tsx         # 67 строк
│   ├── Cases.tsx           # 123 строк
│   ├── AIEnablement.tsx    # 82 строк
│   ├── Approach.tsx        # 92 строк
│   ├── Formats.tsx         # 101 строк
│   ├── Credibility.tsx     # 129 строк
│   └── FinalCTA.tsx        # 137 строк
│
├── cases/
│   └── page.tsx            # Детальные кейсы (5 проектов)
│
└── speaker/
    └── page.tsx            # Спикер-кит (6 тем)
```

## 🎨 Дизайн-система

### Цветовая палитра
```
Primary:   #171717  (Gray 900) - Текст, акценты
Secondary: #FFFFFF  (White)    - Фон
Gray 50:   #F9FAFB             - Альтернативный фон
Gray 100:  #F3F4F6             - Карточки, бейджи
Gray 700:  #374151             - Вторичный текст
```

### Типографика
```
H1:        text-4xl md:text-5xl lg:text-6xl  (36-60px)
H2:        text-3xl md:text-4xl              (30-36px)
H3:        text-xl md:text-2xl               (20-24px)
Body:      text-base md:text-lg              (16-18px)
Small:     text-sm                            (14px)
```

### Spacing
```
Container: max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8
Sections:  py-16 md:py-24
Cards:     p-6 md:p-8
Gap:       gap-4 md:gap-6 lg:gap-8
```

### Breakpoints (Tailwind)
```
sm:  640px   - Small tablets
md:  768px   - Tablets
lg:  1024px  - Laptops
xl:  1280px  - Desktops
```

## 🔄 User Flow

```
Landing (/)
    │
    ├─→ CTA "Обсудить задачу" → Telegram
    │
    ├─→ "Смотреть кейсы" → Scroll to #cases
    │
    ├─→ "Открыть все кейсы" → /cases
    │   └─→ CTA "Обсудить задачу" → Telegram
    │
    ├─→ "Запросить спикера" → /speaker
    │   └─→ CTA "Пригласить спикера" → Telegram
    │
    └─→ Scroll to #contacts
        └─→ Telegram / Copy email
```

## 🎯 Conversion Funnels

### Primary Funnel (Клиент)
```
1. Landing → Hero/Results
2. Scroll → Cases
3. Interest → CTA Click
4. Telegram → Conversation
```

### Secondary Funnel (Спикер)
```
1. Landing → "Запросить спикера"
2. /speaker → Browse Topics
3. Interest → CTA Click
4. Telegram → Booking
```

## 🔍 SEO Architecture

```
┌─────────────────────────────────────┐
│        Technical SEO                │
├─────────────────────────────────────┤
│ • Sitemap.xml                       │
│ • Robots.txt                        │
│ • Canonical URLs                    │
│ • SSR/SSG (Next.js)                 │
│ • Fast loading (< 2s)               │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│        On-Page SEO                  │
├─────────────────────────────────────┤
│ • Meta tags                         │
│ • H1-H3 structure                   │
│ • Alt texts                         │
│ • Schema markup (JSON-LD)           │
│ • OpenGraph / Twitter Cards         │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│      Content Strategy               │
├─────────────────────────────────────┤
│ • Semantic keywords                 │
│ • Long-tail phrases                 │
│ • Internal linking                  │
│ • Regular updates (blog)            │
└─────────────────────────────────────┘
```

## 🚀 Deployment Flow

```
Local Development
    │
    ├─→ git push
    │       │
    │       ▼
    │   CI/CD (GitHub Actions)
    │       │
    │       ├─→ npm install
    │       ├─→ npm run build
    │       ├─→ npm run test
    │       └─→ Deploy
    │               │
    │               ▼
    └─────────→ Production
                    │
                    ├─→ Vercel (рекомендуется)
                    ├─→ Docker + VPS
                    └─→ Netlify / Railway
```

## 📊 Data Flow

```
User Interaction
    │
    ├─→ Button Click
    │       │
    │       ├─→ Analytics Event (GA4/Метрика)
    │       └─→ External Link (Telegram)
    │
    ├─→ Scroll
    │       │
    │       └─→ Scroll Depth Tracking
    │
    └─→ Copy Contact
            │
            └─→ Copy Event + Notification
```

## 🧪 Testing Strategy

```
┌─────────────────────────────────────┐
│      Unit Tests (Future)            │
│  • Component rendering              │
│  • Utility functions                │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│      Integration Tests (Future)     │
│  • Navigation flow                  │
│  • CTA interactions                 │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│      E2E Tests (Future)             │
│  • Full user journey                │
│  • Conversion funnel                │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│      Performance Tests              │
│  • Lighthouse (>90)                 │
│  • Core Web Vitals                  │
│  • Load time (<2s)                  │
└─────────────────────────────────────┘
```

## 🔐 Security Considerations

- ✅ No sensitive data in client
- ✅ External links: `rel="noopener noreferrer"`
- ✅ Email hidden behind button
- ✅ No inline scripts (CSP-ready)
- ✅ HTTPS only (production)

## 📈 Analytics Events (Recommended)

```javascript
// Primary conversions
- "cta_click_hero"
- "cta_click_telegram"
- "cta_click_speaker"

// Navigation
- "scroll_to_cases"
- "scroll_to_contacts"
- "page_view_cases"
- "page_view_speaker"

// Engagement
- "copy_telegram"
- "copy_email"
- "scroll_depth_50"
- "scroll_depth_75"
- "scroll_depth_100"
```

## 🎯 Performance Targets

| Metric | Target | Status |
|--------|--------|--------|
| Lighthouse Score | >90 | ✅ Ready |
| LCP | <2.5s | ✅ SSR |
| FID | <100ms | ✅ Minimal JS |
| CLS | <0.1 | ✅ Reserved space |
| Bundle Size | <200KB | ✅ Next.js |

## 🌍 Internationalization (Future)

```
Current: ru_RU only
Future:
    ├─→ en_US (Q2 2025)
    ├─→ Routing: /en/...
    └─→ i18n: next-intl
```

---

**Last Updated:** 2024-12-23
**Version:** 1.0.0
